const employ = require("../index");

// Not working
describe("index", () => {
    describe("Menu", () => {
        it("Starts with function called openMenu", () => {
    
          expect(result instanceof Manager).toEqual(true);
        });
      });
  });
  